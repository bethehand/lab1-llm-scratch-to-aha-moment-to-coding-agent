实验一：
从完整构建大模型到追顿悟到构建coding agent


从Stanford CS336 Language Modeling from Scratch开始，学习大语言模型，并实践。学了CS336后，我想自己实现下，刚好手上有4张4090GPU。
第一，我想从头复现LLM的训练完整流程，从预训练到SFT到RL。所以我在4张4090GPU，用fine web edu上训练了一个95M参数的模型，发现能力比较差。后面又训练了一个201M（0.2B）参数的模型，基于这个模型做了SFT（apaca数据），又做了RL（GSM8K）。在SFT和RL上都能感觉到模型有提升，任务上pass@1的提升。
第二，在训练模型中，了解到了deepseek R1的“顿悟”时刻，感觉很神奇，想要自己复现“顿悟”时刻。开始追“顿悟”的历程。用的Qwen2.5 Base模型直接在上面做RL训练追顿悟。用countdown 3的题目去训。第一次rl训练后没出现顿悟，原因是模型走了“捷径”，虽然pass@1提高，但token量变少很多，鼓励猜一次就中，而不是鼓励多想。 后面想着堵住捷径才有可能复现顿悟，就用了一半GSM8K的题目和一半countdown3的题目做RL训练（混训）。在混训后，发现捷径堵住了，而且出现了顿悟的措辞。后面继续用一半GSM8K的题目和一半countdown4的题目继续做RL训练，换题是为了换更需要搜索的场景，让顿悟更好地复现。这时候顿悟出现了，追顿悟成功了。出现了顿悟的措辞（too low too high），而且这个措辞是有用的，而不是装饰，但就是token长度没有变长，还是一样。（思维链本质是用更多token换对具体问题的更多算力）
第三，这时候想着更好的解决countdown 4的问题，提高解决这问题的pass@1。所以做了三个实验对照，第一个，继续RL；第二个，自蒸馏后SFT再RL训练；第三个，程序老师写教材给模型，SFT后再RL训练。这三个实验，第三个最强，第二个次之，第一个最差。这也是第一次做教材给SFT训练，而且体会到了SFT是便宜有效的做法。此时对于countdown4的pass@1的数据是0.6左右。后面继续提升，引入工具计算器，也是第一次用harness。先SFT教用计算器，再RL训练稳定使用计算器。但RL后没有SFT后效果好，所以改了环境设计的秤，题（离线采样筛选题目），锚，后面RL就比SFT好了，这时候pass@1来到了0.75左右。
后面想彻底解决这个任务，达到可用的产品级别，引入了deepseek工具，也是先sft教去<ask>，再RL做稳定。直接在countdown4任务的pass@1上做到了0.95，基本解决的countdown4这个任务。
第四，在做实验的过程中了解到，引入工具并不是真正的与环境交互，碰撞。真正的环境是有状态，收动作，给反馈，想和真正的环境碰撞。这时候就做了猜数字的实验。 猜数字的实验不仅能收动作，给反馈，环境本身也有状态。
第五，做完与真实环境产生碰撞的猜数字游戏后，我想做一个真正的有用的agent。 Agent有4个特点：长程，与真正的环境碰撞交互，弱观测（自己验证），控制回路。所以我就涉及了做以下4个实验：第一是短程强观测的修bug实验。第二是长程强观测的bug拼包实验。第三个是长程强观测的真实环境实验，做的是exercism题。最后一个是长程弱观测实验，拿走环境的<test>结果，模型有工具，但是需要模型自己写测试，然后自己用工具去验证，即模型自己验证。
第六，之后又学习了stanford cs329A：self improving ai agents. 知道了4台发动机，分别是预训练scaling，SFT塑造角色，test time scaling和闭环。其中test time scaling的串行和并行采样出的和环境发生碰撞交互的轨迹很重要，能选出好的轨迹的便宜的验证器也很重要，这样好的轨迹又能回流回模型，形成闭环，self improving。

工程基建方面：infra把4卡4090GPU用到极致，用vllm，4卡跑模型每一步做一次all reduce。vllm只在all reduce前等最后一条最慢的rollout，中间不等待的on policy rollout。下一步如果要加快的话只能就是轻微off policy了。

数据方面：从：fine web edu用于预训练 到 alpaca 用于SFT 到 GSM8K用于RL。再到countdown3/4用于RL训练去追顿悟，gsm8k+countdown3/4混训，再到猜数字，最后到coding任务。

几条重要的感悟：
1.能力扩张需要新信息进入系统，来源只有两个：外部数据 以及 信息量的碰撞。同时必须保住多样性。
外部数据有两个：1是预训练的零件 2是SFT的教材（其实现在两个界线已经比较模糊，还有一个mid train）
信息量的碰撞有两个：1.环境设计（包括验证器），与外部环境进行碰撞和交互。 2.用算力大量采样（探索算力）：串行采样和并行采样。

2.预训练看val loss指标 sft和RL看任务完成的怎么样：pass@1 pass@32

3.预训练给零件（电路）
SFT给流程（现在pre train ，sft， mid train已经界线模糊）
RL给偏好做可靠+开“新”路（环境碰撞，算力探索）（验证器挑出来）

4.便宜的验证器很重要：因为在有算力有好的大脑（预训练模型）后，采样得出来，但不一定筛选的出来（认的出来才能留下来， 留下来后还能给到mid train，形成闭环），需要便宜的“完美”验证器。
秤（有一部分就是验证器）也很重要，RL训练出来的就是秤的口味。
环境也很重要，现在工业界很多都在做环境设计，环境设计是RL的工作大头。

5.算力关键：
有了算力就像奇异博士（doctor stranger），能推演出多个未来，从其中选择一个想要的未来，并实现它。

6.RL的信息量只有1bit （当然RL找到轨迹后去给到mid train信息量就很大了）
小信息也能撬动大本事（完成大任务），前提零件都在。

7.RL的开路能力是“支撑集的密度（零件）”*“环境反馈的密度（环境）”*“采样预算（算力）”三样的乘积，预训练给第一样，环境给第二样，算力给第三样。 RL：调偏好，找拼法，装回路（多轮环境，装进去的是决定，不是事实）
外部信息来自验证器和环境，秤（验证器）的判决进权重，环境的观测进上下文。

8.秤：稀疏和稠密
RL训练更像开关和密码锁，有p，如果p为0，则完全没有信号。
稀疏奖励（开关秤） 稠密奖励（尺子秤）   稀疏目标（另一件事 p很小的意思）
